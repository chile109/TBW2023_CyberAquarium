export type ensDataType = {
  ensName?: string,
  avatarUrl?: string,
  twitter?: string,
  github?: string,
  websiteUrl?: string
  email?: string,
  ethAddress?: string,
}

export type nftsData ={
  identifier: number;
  image_original_url?: string;
  image_preview_url?: string;
  image_url?: string;
}